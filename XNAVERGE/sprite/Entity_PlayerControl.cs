using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace XNAVERGE {
    public partial class Entity {/*
        protected virtual void control_player(int elapsed) {
            int x = 0, y = 0;
            if (VERGEGame.game.dir.left.down) x--;
            if (VERGEGame.game.dir.right.down) x++;
            if (VERGEGame.game.dir.up.down) y--;
            if (VERGEGame.game.dir.down.down) y++;
            if (x == 0 && y == 0) {
                if (_moving) set_walk_state(false);
            }
            else {
                if (!_moving) set_walk_state(true);
                facing = Utility.direction_from_signs(x, y, false);
                movement_direction = Utility.direction_from_signs(x, y, true);
                try_to_move(elapsed, true);
            }
        }*/
    }
}
